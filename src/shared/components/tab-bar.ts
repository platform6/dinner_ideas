/**
 * The phone tab bar's height (`Layout.tsx`). Its own module so anything sticky or scrolled on a
 * phone can clear the bar without importing the whole app shell, and its auth, along with it.
 */
export const TAB_BAR_HEIGHT_PX = 70;
